#include<stdio.h>

#define MAXN 10005
#define MAXM 1005

int main(){
    int n,i;
    char s[MAXM];
  //  scanf("%d", &n);
    //char s[MAXM];
    for(i=0;i<3;i++){
        scanf("%[^\n]%*c", s); 
    }
   /* for(i=0;i<10;i++){
        printf("%c", s[i]);
    } */ 
    return 0;
}